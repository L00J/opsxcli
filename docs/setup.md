# setup

opsxcli setup — 配置 LLM 提供商

## 用法

`opsxcli setup`

## 说明

交互式配置管理工具，用于管理 LLM（大语言模型）提供商设置。支持添加新提供商、修改已有配置、设置默认提供商、查看当前配置状态。首次运行时自动进入配置向导，引导完成初始配置。

此命令为纯交互式操作，无命令行标志。

## 选项

无。所有配置通过交互式界面完成。

## 示例

```bash
# 首次配置，进入配置向导
opsxcli setup

# 修改已有配置
opsxcli setup

# 查看当前配置状态
opsxcli setup
```
