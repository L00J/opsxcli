# ifconfig

opsxcli ifconfig — 显示网络接口信息

## 用法

`opsxcli ifconfig`

## 说明

显示系统所有网络接口的配置信息，包括 IP 地址、MAC 地址、状态等。此命令为只读显示，不支持修改网络接口配置（如 up/down、设置 IP 地址等）。

## 选项

无。

## 示例

```bash
# 显示所有网络接口信息
opsxcli ifconfig
```
