# 02 - 整体架构设计

## 📁 项目结构

```
opsxcli/
├── cmd/                    # 命令行接口层
│   ├── root.go            # 根命令和帮助系统
│   ├── builtin.go         # Builtin 内置命令定义
│   ├── mysql.go           # 数据库命令定义
│   ├── ssh.go             # SSH 命令定义
│   └── ...                # 其他命令定义
│
├── plugins/               # 功能实现层
│   ├── builtin/           # Builtin 内置命令 Go 原生实现（基础命令）
│   │   ├── fileops.go    # 文件操作 (ls, cat, cp, mv, rm, mkdir, touch, grep)
│   │   └── network.go    # 网络配置 (ifconfig, route, ip)
│   │
│   ├── ping/             # 网络诊断工具（独立插件）
│   ├── traceroute/       # 路由追踪
│   ├── telnet/           # Telnet 客户端
│   ├── nc/               # Netcat
│   ├── netstat/          # 网络统计（含 ss）
│   ├── nmap/             # 端口扫描
│   ├── wget/             # 文件下载
│   │
│   ├── mysql/            # 数据库客户端（独立插件）
│   ├── postgres/         # PostgreSQL 客户端
│   ├── redis/            # Redis 客户端
│   │
│   ├── ssh/              # SSH 客户端（独立插件）
│   │
│   ├── sys/              # 系统监控实现（TUI）
│   ├── net/              # 网络监控实现（TUI）
│   │
│   ├── server/           # HTTP/gRPC 服务器
│   ├── request/          # HTTP 请求工具
│   └── install/          # 安装工具
│
├── internal/             # 内部核心模块（不可外部导入）
│   ├── agent/            # AI Agent 核心
│   │   ├── core/         #   Agent 引擎（ReAct 循环）
│   │   ├── prompt/       #   Prompt 构建器（五层架构）
│   │   ├── tools/        #   工具注册中心 + 内置工具
│   │   ├── safety/       #   安全控制器
│   │   ├── session/      #   会话管理
│   │   ├── evolver/      #   自我进化引擎
│   │   └── tui/          #   Agent TUI 界面
│   ├── llm/              # LLM 客户端（OpenAI/Claude/Gemini/DeepSeek/Ollama）
│   ├── config/           # 配置管理
│   ├── db/               # 数据库持久化（SQLite）
│   ├── logger/           # 统一日志系统
│   ├── memory/           # 记忆管理
│   ├── security/         # 安全防护
│   ├── tui/              # 通用 TUI 组件
│   ├── ui/               # UI 主题和基础组件
│   ├── sshconfig/        # SSH 配置解析
│   ├── output/           # 输出格式化（text/json）
│   ├── auth/             # JWT 认证
│   └── core/             # 通用引擎
│
└── main.go               # 程序入口
```

## 🏗️ 架构设计原则

### 1. 分层架构

- **命令层 (cmd/)**: 使用 Cobra 框架定义命令接口、参数解析和帮助文档
- **实现层 (plugins/)**: 包含各功能的具体实现逻辑
- **工具层 (internal/)**: 提供通用工具函数和辅助功能

### 2. 插件化设计

每个功能模块都是一个独立的 plugin，遵循以下规范：

```
plugins/<功能名>/
├── <主文件>.go     # 主要实现
├── types.go        # 类型定义（如有）
├── utils.go        # 辅助函数（如有）
└── ...             # 其他相关文件
```

**插件命名规范**:
- 包名与目录名一致
- 对外导出的函数使用大写开头
- 内部函数使用小写开头

### 3. Builtin 集成策略

Builtin 内置命令采用**混合模式**：

#### plugins/builtin/ - 内置命令 Go 原生实现
**文件操作** (fileops.go):
- ls, cat, cp, mv, rm, mkdir, touch, grep

**网络配置** (network.go):
- ifconfig, route, ip

**特点**:
- 完全独立，无需系统命令
- 跨平台兼容
- 可在最小化容器中运行
- 体积增加约 3-5MB

#### 独立 plugins/ - 复杂工具保持独立
**网络诊断工具** (虽是基础工具，但实现复杂):
- ping, traceroute, telnet, nc, netstat
- 原因：包含完整协议实现、多种fallback机制、复杂的错误处理

**应用层工具** (非 builtin 范畴):
- mysql, postgres, redis - 数据库客户端
- ssh - SSH 客户端和 SFTP
- nmap - 端口扫描
- wget - HTTP 下载

**TUI 监控** (opsxcli 特色功能):
- sys - 系统监控
- net - 网络监控

**其他工具**:
- server - HTTP/WebSocket/gRPC 服务
- request - HTTP 请求工具
- install - 安装脚本

#### 系统命令转发（cmd/ 中通过 createForwardCmd 实现）
- **编辑工具**: vi, vim, awk, sed
- **压缩工具**: tar, gzip, unzip
- **进程管理**: ps, kill, pstree
- **系统信息**: uname, hostname, free, df, du

**优势**:
- 不增加二进制体积
- 保持与系统命令完全兼容
- 利用系统已有功能

---

### 4. 插件分类标准

**放入 plugins/builtin/** 的条件（需**同时**满足）:
1. ✅ 属于 POSIX 基础命令
2. ✅ Go 标准库可实现（或仅需轻量库如 gopsutil）
3. ✅ 代码简洁（单文件 < 500 行）
4. ✅ 功能明确，无复杂协议

**保持独立 plugins/** 的条件（满足**任一**）:
1. ❌ 实现复杂（> 500 行）
2. ❌ 需要专门协议处理
3. ❌ 需要外部依赖
4. ❌ 应用层工具
5. ❌ opsxcli 特色功能

**详细说明**: 参见 `docs/PLUGIN_ARCHITECTURE.md`

## 📊 代码组织

### 命令注册流程

```go
// 1. 在 cmd/builtin.go 中定义命令
func NewLsCmd() *cobra.Command {
    var opts builtin.LsOptions
    cmd := &cobra.Command{
        Use:   "ls [flags] [files...]",
        Short: "列出目录内容",
        RunE: func(cmd *cobra.Command, args []string) error {
            return builtin.Ls(args, opts)  // 调用 plugin 实现
        },
    }
    cmd.Flags().BoolVarP(&opts.All, "all", "a", false, "显示隐藏文件")
    return cmd
}

// 2. 在 cmd/root.go 中注册命令
rootCmd.AddCommand(
    NewLsCmd(),
    NewCatCmd(),
    // ...
)

// 3. 在 plugins/builtin/fileops.go 中实现功能
func Ls(paths []string, opts LsOptions) error {
    // 实现 ls 命令逻辑
}
```

### 帮助系统组织

帮助信息按功能分组显示（cmd/root.go）：

```go
groups := []commandGroup{
    {"文件", []string{"ls", "cat", "grep", "vi"}, "文件和目录操作"},
    {"数据库", []string{"mysql", "psql", "redis"}, "MySQL, PostgreSQL, Redis"},
    {"系统", []string{"ps", "top", "free", "df"}, "进程和系统信息"},
    {"网络", []string{"ssh", "ping", "nc", "ss"}, "SSH, Ping, 端口扫描等"},
    {"监控", []string{"sys", "net"}, "系统监控, 网络监控"},
}
```

---

## 🔄 核心流程图

### CLI 命令行参数解析流程

```
用户输入: opsxcli --profile prod agent "检查磁盘空间"
         │
         ▼
┌─────────────────────────────────┐
│  main.go                        │
│  os.Args → cobra.OnInitialize   │
│  ├─ 加载配置 (internal/config)   │
│  ├─ 初始化日志 (internal/logger) │
│  └─ 设置 profile 环境           │
└──────────────┬──────────────────┘
               │
               ▼
┌─────────────────────────────────┐
│  rootCmd.Execute()              │
│  cobra 命令路由                  │
│                                 │
│  PersistentFlags (全局):         │
│  ├─ --profile   选择配置档案     │
│  ├─ --debug     调试模式         │
│  └─ --output    输出格式         │
└──────────────┬──────────────────┘
               │
         ┌─────┴─────┬──────────┬──────────┐
         ▼           ▼          ▼          ▼
    ┌─────────┐ ┌────────┐ ┌──────┐ ┌────────┐
    │ ls      │ │ mysql  │ │ sys  │ │ agent  │
    │ cp/mv/rm│ │ psql   │ │ net  │ │ session│
    │ cat/grep│ │ redis  │ │ ping │ │ setup  │
    │ ...     │ │ ssh    │ │ ...  │ │ ...    │
    └────┬────┘ └───┬────┘ └──┬───┘ └───┬────┘
         │          │         │         │
         ▼          ▼         ▼         ▼
    ┌────────────────────────────────────────┐
    │  plugins/<plugin>/  功能实现层          │
    │  ├─ builtin.Ls(args, opts)              │
    │  ├─ mysql.Connect(host, port, ...)      │
    │  ├─ sys.NewMonitor().Run()              │
    │  └─ agent.Run(query, opts)              │
    └────────────────────────────────────────┘
```

### Agent 完整执行流程

以 `opsxcli agent "安装 mysql 数据库"` 为例：

```
用户输入: opsxcli agent "安装 mysql 数据库"
         │
         ▼
┌──────────────────────────────────────────────────┐
│  1. 命令解析 (cmd/agent.go)                       │
│     ├─ 读取 flags: --provider, --model, --session │
│     ├─ 加载配置 (~/.opsxcli/config.yaml)          │
│     └─ 解析 query 参数                            │
└────────────────────┬─────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────┐
│  2. 初始化 Agent (internal/agent/core/)           │
│     ├─ 选择 LLM Provider (OpenAI/Claude/Ollama)  │
│     ├─ 加载工具注册表 (internal/agent/tools/)      │
│     ├─ 恢复/创建会话 (internal/agent/session/)     │
│     └─ 注入记忆 (internal/memory/)                │
└────────────────────┬─────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────┐
│  3. Prompt 构建 (internal/agent/prompt/)          │
│     ┌──────────────────────────────────┐         │
│     │ Layer 1: 系统角色定义             │         │
│     │   "你是 opsxcli，智能运维助手"    │         │
│     ├──────────────────────────────────┤         │
│     │ Layer 2: 安全规则                 │         │
│     │   危险操作审批、文件保护          │         │
│     ├──────────────────────────────────┤         │
│     │ Layer 3: 工具描述                 │         │
│     │   bash, ssh, mysql, redis...     │         │
│     ├──────────────────────────────────┤         │
│     │ Layer 4: 上下文记忆               │         │
│     │   历史会话摘要、用户偏好          │         │
│     ├──────────────────────────────────┤         │
│     │ Layer 5: 用户输入                 │         │
│     │   "安装 mysql 数据库"             │         │
│     └──────────────────────────────────┘         │
└────────────────────┬─────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────┐
│  4. ReAct 循环 (迭代执行)                          │
│                                                   │
│  ┌─────────────┐                                  │
│  │ LLM API 调用 │ ← messages[] 完整对话历史        │
│  └──────┬──────┘                                  │
│         │                                         │
│    ┌────┴────┐                                    │
│    │ 解析响应 │                                    │
│    └────┬────┘                                    │
│         │                                         │
│    ┌────┴──────────────┐                          │
│    │                   │                          │
│    ▼                   ▼                          │
│  [文本回复]        [Tool Calls]                    │
│    │                   │                          │
│    ▼                   ▼                          │
│  返回用户      ┌───────────────┐                  │
│                │ 工具注册表查找  │                  │
│                │ tools[name]   │                  │
│                └───────┬───────┘                  │
│                        │                          │
│              ┌─────────┴──────────┐               │
│              │                    │               │
│              ▼                    ▼               │
│         [内置工具]           [外部命令]            │
│         bash.Exec()        ssh.Run()             │
│         mysql.Query()      install.Run()         │
│              │                    │               │
│              └────────┬───────────┘               │
│                       │                           │
│                       ▼                           │
│              ┌─────────────────┐                  │
│              │ ToolCallback    │                  │
│              │ TUI 实时显示:    │                  │
│              │ 🔧 → ✅ (1.2s)  │                  │
│              └────────┬────────┘                  │
│                       │                           │
│                       ▼                           │
│              结果追加到 messages[]                  │
│                       │                           │
│                       └──→ 回到 LLM API 调用 ──→  │
│                                                   │
│  直到 LLM 不再返回 tool_calls                      │
└────────────────────┬─────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────┐
│  5. 后处理                                        │
│     ├─ 保存会话到 JSONL (session/)                 │
│     ├─ 记忆更新 (memory/)                          │
│     ├─ Evolver 学习 (evolver/)                     │
│     └─ Markdown 渲染输出                           │
└──────────────────────────────────────────────────┘
```

### 系统数据流

```
┌──────────────── cmd 层 ────────────────┐
│                                         │
│  root.go ──→ agent.go ──→ builtin.go   │
│               │              │          │
└───────────────┼──────────────┼──────────┘
                │              │
                ▼              ▼
┌──────── internal 层 ──────────────────┐
│                                        │
│  agent/core ←─→ agent/tools ←─→ llm/  │
│      │              │                  │
│      ▼              ▼                  │
│  agent/prompt   agent/session          │
│  agent/safety   agent/evolver          │
│  memory/        config/                │
│                                        │
└────────────────┬───────────────────────┘
                 │
                 ▼
┌──────── plugins 层 ──────────────────┐
│                                       │
│  builtin/   mysql/   ssh/   sys/     │
│  ping/      redis/   net/   wget/    │
│  ...                                   │
│                                       │
└───────────────────────────────────────┘
```

## 🔧 技术栈

- **CLI 框架**: [cobra](https://github.com/spf13/cobra)
- **TUI 框架**: [Bubble Tea](https://github.com/charmbracelet/bubbletea)
- **系统信息**: [gopsutil](https://github.com/shirou/gopsutil)
- **SSH 客户端**: [golang.org/x/crypto/ssh](https://pkg.go.dev/golang.org/x/crypto/ssh)
- **数据库客户端**: 各数据库官方 Go 驱动

## 📈 性能优化

### 二进制体积控制

```bash
# 编译时使用 ldflags 剥离调试信息
go build -ldflags="-s -w" -o opsxcli main.go
```

**体积优化效果**:
- 优化前: ~22MB
- 优化后: ~15MB
- 减少: ~32%

### 实时监控优化

- `sys` 和 `net` 监控采用 2 秒刷新间隔
- 使用 goroutine + ticker 实现异步数据采集
- TUI 使用双缓冲避免闪烁

## 🎯 设计目标

1. **运维瑞士军刀**: 集成常用运维工具，减少工具切换
2. **完全独立**: 核心功能无需依赖系统命令
3. **体积适中**: 在功能丰富和体积控制间取得平衡
4. **易于扩展**: 插件化设计便于添加新功能
5. **用户友好**: 统一的命令接口和详细的帮助文档

## 🚀 添加新命令

### 添加 Go 原生实现命令

1. 在 `plugins/builtin/fileops.go` 或新文件中实现功能：
```go
func NewCommand(args []string, opts Options) error {
    // 实现逻辑
}
```

2. 在 `cmd/builtin.go` 中定义 Cobra 命令：
```go
func NewNewCommandCmd() *cobra.Command {
    // 定义命令
}
```

3. 在 `cmd/root.go` 中注册命令：
```go
rootCmd.AddCommand(NewNewCommandCmd())
```

### 添加系统命令转发

```go
func NewCommandCmd() *cobra.Command {
    return createForwardCmd("command", "简短描述", "详细描述")
}
```

## 📝 文档规范

- `README.md`: 项目介绍和快速开始
- `ARCHITECTURE.md`: 架构设计说明（本文档）
- `BUILTIN_COMMANDS.md`: Builtin 集成规划
- `CODE_QUALITY_REPORT.md`: 代码质量报告
