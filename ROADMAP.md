# 🗺️ OpsXCLI 开发路线图

> **版本**: v1.1  
> **更新日期**: 2026-04-21  
> **基于**: master 分支最新提交  
> **当前版本状态**: v0.4.x (紧急修复+安全加固)

---

## 📋 目录

- [版本总览表](#版本总览表)
- [当前版本 v0.4.x 详细任务](#当前版本-v04x-详细任务)
- [下一版本 v0.5.0 详细任务](#下一版本-v050-详细任务)
- [项目概览](#项目概览)
- [技术栈](#技术栈)
- [架构设计](#架构设计)
- [Agent V2 自进化架构](#agent-v2-自进化架构)
- [GEP 自进化增强规划](#gep-自进化增强规划)
- [代码审计发现项](#代码审计发现项)
- [版本里程碑](#版本里程碑)
- [技术债务清理计划](#技术债务清理计划)

---

## 版本总览表

| 版本 | 目标日期 | 主题 | 关键指标 | 状态 |
|------|----------|------|----------|------|
| **v0.4.x** | 2026-04 | 紧急修复+安全加固 | 修复C1/C2, CI门禁→30% | 🔧 进行中 |
| **v0.5.0** | 2026-05 | Agent自进化V2 | 统一execute/transfer, GEP引擎 | 📋 规划中 |
| **v0.6.0** | 2026-05 | Skill系统 | 种子Skill+自动提炼 | 📋 规划中 |
| **v0.8.0** | 2026-06 | 运维工具补齐 | Docker管理, 告警通知 | 📋 规划中 |
| **v1.0.0** | 2026-10 | 生产就绪 | 覆盖率>50%, 文档站 | 📋 规划中 |

---

## 当前版本 v0.4.x 详细任务

### 🔧 紧急修复 (P0)

| 编号 | 问题 | 文件位置 | 说明 | 状态 |
|------|------|----------|------|------|
| **C1** | 命令注入漏洞 | `plugins/nc/nc.go:95` | 用户输入未转义，可执行任意命令 | ✅ 已修复 |
| **C2** | 测试分类失败 | `internal/llm/factory_test.go` | glm/minimax提供商分类测试用例失败 | ✅ 已修复 |
| **W4** | Evolver结果丢弃 | `agent.go:405` | 10步进化循环结果未被消费/反馈 | ✅ 已修复 |

**修复优先级**: C1(安全) > C2(测试) > W4(功能)

### 🛡️ 安全加固 (P1)

| 编号 | 问题 | 文件位置 | 说明 | 状态 |
|------|------|----------|------|------|
| **W1** | 全局密码变量 | `cmd/common.go:12` | 密码存储在全局变量，存在泄露风险 | ✅ 已修复 |

**加固计划**:
- [x] 将全局密码变量改为局部传递 + 内存用后即清
- [x] C1 修复后添加命令注入回归测试
- [x] CI 门禁从 15% → 30%

### 🧪 测试补齐 (P1)

- [x] 修复 C2: `factory_test.go` glm/minimax 分类测试
- [x] 添加 `plugins/nc/` 命令注入安全测试
- [x] `plugins/` 层基础覆盖 0% → 20%（22/26包≥20%，平均49%）
- 详见 [TDD_PLAN.md](./TDD_PLAN.md)

---

## 下一版本 v0.5.0 详细任务

### 🔧 Agent自进化V2 (P0)

> **目标日期**: 2026-05 | **优先级**: 短期(3-5天)

| 任务 | 说明 | 预估工时 | 状态 |
|------|------|----------|------|
| **统一 execute 工具** | 合并 local_bash + ssh_execute 为统一接口，本地/远程自动路由 | 2天 | ✅ 已完成 |
| **统一 transfer 工具** | 合并文件传输接口，本地cp/远程scp自动选择 | 1天 | ✅ 已完成 |
| **风险审批集成** | 安全控制器与统一工具联动，高危操作自动审批流程 | 2天 | 📋 规划中 |

### 🧠 GEP自进化引擎 (P1)

> **目标日期**: 2026-05 | **优先级**: 中期(1-2周)

| 任务 | 说明 | 预估工时 | 状态 |
|------|------|----------|------|
| **三层记忆架构** | 事实层(MEMORY.md+USER.md) + 程序层(SKILL_xxx.md) + 历史层(events.jsonl) | 3天 | 📋 规划中 |
| **种子Skill内置** | local_common_ops, install_software, risk_approval, network_diagnosis, basic_recovery | 3天 | 📋 规划中 |
| **Skill自动提炼** | 复杂任务后(≥5次工具调用)自动提炼 SKILL.md | 2天 | 📋 规划中 |
| **10步循环优化** | 仅复杂任务触发进化，简单任务跳过以节省Token | 1天 | 📋 规划中 |

### 📊 增强TUI进化展示 (P2)

> **目标日期**: 2026-06

| 任务 | 说明 | 状态 |
|------|------|------|
| 进化过程可视化 | 10步循环状态实时展示 | 📋 规划中 |
| Skill库浏览 | TUI中查看/搜索/管理已习得Skill | 📋 规划中 |
| LLM插件切换 | 运行时切换LLM提供商 | 📋 规划中 |

---

## 项目概览

OpsXCLI 是面向运维和开发的集成化命令行工具集，整合了 **70+ 运维命令**，涵盖数据库操作、网络调试、系统监控、容器管理、AI 运维助手等功能。采用纯 Go 实现，单二进制分发，零外部依赖。

**核心定位**: 运维瑞士军刀 — 一条命令操作 MySQL、Redis、SSH、Docker、Kubernetes 等服务。

---

## 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| **Go** | 1.24.2 | 主语言，CGO_ENABLED=0 静态编译 |
| **Cobra** | v1.8.0 | CLI 命令框架，参数解析与子命令管理 |
| **Bubble Tea** | v1.3.10 | 终端 UI 框架，TUI 交互界面 |
| **modernc.org/sqlite** | v1.41.0 | 纯 Go SQLite 驱动，零 CGO 依赖 |
| **Lipgloss** | v1.1.1 | 终端样式渲染 |
| **Glamour** | v0.10.0 | Markdown 终端渲染 |
| **gopsutil** | v3.24.1 | 系统信息采集 |
| **go-redis** | v8.11.5 | Redis 客户端 |
| **go-sql-driver/mysql** | v1.7.1 | MySQL 驱动 |
| **lib/pq** | v1.10.9 | PostgreSQL 驱动 |
| **golang.org/x/crypto** | v0.46.0 | SSH 协议实现 |

---

## 架构设计

### 三层架构

```
┌─────────────────────────────────────────────────┐
│                    cmd/ 层                       │
│         命令入口 — 参数解析与路由                   │
│   root.go, mysql.go, redis.go, ssh.go, ...      │
├─────────────────────────────────────────────────┤
│                  plugins/ 层                     │
│         功能插件 — 独立可用，可单独测试              │
│   mysql/, redis/, ssh/, docker/, kubernetes/...  │
├─────────────────────────────────────────────────┤
│                 internal/ 层                     │
│         内部核心 — 基础设施与通用模块               │
│   config/, db/, logger/, agent/, llm/, tui/...   │
└─────────────────────────────────────────────────┘
```

### 依赖规则

| 允许方向 | 说明 |
|----------|------|
| `cmd/` → `plugins/` | 命令层调用插件层 |
| `cmd/` → `internal/` | 命令层使用基础设施 |
| `plugins/` → `internal/` | 插件层使用基础设施 |
| `plugins/` → `cmd/` | ❌ **禁止** |
| `internal/` → `plugins/` | ❌ **禁止** |

### 关键设计原则

- **单一职责**: 每个模块只负责一个明确功能
- **插件独立性**: 每个插件可独立运行和测试
- **静态编译**: `CGO_ENABLED=0`，无 glibc 依赖，跨平台兼容
- **统一配置**: `~/.opsxcli/config.yaml` + 环境变量 + 命令行参数

---

## Agent V2 自进化架构

> **已实现** — Agent 核心架构已完成，进入优化增强阶段

### Evolver 10步进化循环

```
OBSERVE → EXTRACT → REFLECT → SCORE → COMPARE → LEARN → STORE → CONSOLIDATE → PREDICT → ADAPT
```

- **OBSERVE**: 观察工具调用输出
- **EXTRACT**: 提取关键信息
- **REFLECT**: 反思执行过程
- **SCORE**: 对结果评分
- **COMPARE**: 与历史经验对比
- **LEARN**: 归纳学习
- **STORE**: 存储经验到记忆
- **CONSOLIDATE**: 整合已有知识
- **PREDICT**: 预测后续步骤
- **ADAPT**: 适应当前环境

### 三层记忆系统

| 层级 | 存储 | 用途 |
|------|------|------|
| **经验层** | events.jsonl | 历史执行记录，结构化事件日志 |
| **环境层** | env.json | 运行环境快照，系统/网络/服务状态 |
| **对话层** | 对话历史 | 上下文窗口内的会话记忆 |

### 6个内置工具

| 工具 | 说明 | 暴露方式 |
|------|------|----------|
| `local_bash` | 本地命令执行 | → 将合并为统一 `execute` |
| `ssh_execute` | SSH远程命令执行 | → 将合并为统一 `execute` |
| `scp_transfer` | 文件传输 | → 将合并为统一 `transfer` |
| `analyze_output` | 输出分析 | 保留 |
| `file_read` | 文件读取 | 保留 |
| `file_search` | 文件搜索 | 保留 |

### 五层Prompt架构

```
Layer 1: System Identity    — 身份与角色定义
Layer 2: Memory Context     — 三层记忆注入
Layer 3: Skill Context      — 已习得技能加载
Layer 4: Safety Rules       — 安全控制器规则
Layer 5: Task Context       — 当前任务与工具描述
```

### 安全控制器

| 维度 | 说明 |
|------|------|
| **4级风险** | 低/中/高/严重，自动评级命令风险 |
| **3级模式** | 正常/观察/严格，控制审批策略 |
| **审计** | 全量操作日志，可追溯 |

---

## GEP 自进化增强规划

> **GEP** = Generalized Evolution Pattern，通用进化模式

### 架构演进图

```
v0.4.x (当前)                    v0.5.0 (目标)                    v0.6.0 (增强)
┌──────────────┐               ┌──────────────┐               ┌──────────────┐
│ 6个独立工具    │    ──→       │ 统一工具接口   │    ──→       │ Skill 系统    │
│ local_bash    │               │ execute(统一) │               │ 种子Skill ×5  │
│ ssh_execute   │               │ transfer(统一)│               │ 自动提炼      │
│ scp_transfer  │               │              │               │ SKILL.md管理  │
└──────────────┘               └──────────────┘               └──────────────┘
┌──────────────┐               ┌──────────────┐               ┌──────────────┐
│ 三层记忆      │    ──→       │ 增强三层记忆   │    ──→       │ 完整记忆系统  │
│ 经验JSONL    │               │ +事实层       │               │ 语义检索      │
│ 环境JSON     │               │ +程序层(Skill)│               │ 记忆衰减      │
│ 对话历史     │               │ +历史层       │               │ 跨会话共享    │
└──────────────┘               └──────────────┘               └──────────────┘
```

### 统一工具设计

**execute 工具** (取代 local_bash + ssh_execute 暴露给 LLM):
- 自动检测目标：无 host 参数 → 本地执行，有 host 参数 → SSH 远程执行
- 统一参数：`{command: string, host?: string, timeout?: int, sudo?: bool}`
- 统一返回：`{stdout, stderr, exit_code, duration}`

**transfer 工具** (统一文件传输):
- 本地：直接 cp
- 远程：自动走 scp
- 参数：`{src, dst, direction: "upload"|"download", host?: string}`

### 增强三层记忆

| 层级 | 存储 | 内容 | 生命周期 |
|------|------|------|----------|
| **事实层** | MEMORY.md + USER.md | 用户偏好、环境事实、常用配置 | 永久 |
| **程序层** | SKILL_xxx.md | 操作技能文档（步骤、参数、注意事项） | 版本化 |
| **历史层** | events.jsonl | 执行事件日志 | 可清理 |

### 内置种子Skill (v0.6.0)

| Skill | 说明 | 触发场景 |
|-------|------|----------|
| `local_common_ops` | 常用本地操作集合 | 文件管理、进程管理、服务管理 |
| `install_software` | 软件安装标准化流程 | apt/yum/brew 一键安装 |
| `risk_approval` | 高危操作审批流程 | rm -rf、重启服务、修改配置 |
| `network_diagnosis` | 网络故障诊断 | 连接异常、DNS问题、防火墙排查 |
| `basic_recovery` | 基础故障恢复 | 服务重启、日志清理、磁盘空间回收 |

### Skill 自动提炼

- **触发条件**: 单次任务 ≥ 5次工具调用
- **提炼流程**: 执行日志 → LLM 总结 → SKILL.md 生成 → 人工确认(可选)
- **版本管理**: Skill 文件带版本号，支持回滚

---

## 代码审计发现项

### 紧急修复项 (Critical/Warning)

| 编号 | 严重度 | 文件 | 行号 | 问题 | 修复方案 | 状态 |
|------|--------|------|------|------|----------|------|
| **C1** | 🔴 Critical | `plugins/nc/nc.go` | 95 | 命令注入漏洞 | 输入转义+白名单 | ✅ 已修复 |
| **C2** | 🔴 Critical | `internal/llm/factory_test.go` | — | glm/minimax分类测试失败 | 修复测试用例 | ✅ 已修复 |
| **W1** | 🟡 Warning | `cmd/common.go` | 12 | 全局密码变量 | securePassword[]byte+用后清零 | ✅ 已修复 |
| **W4** | 🟡 Warning | `agent.go` | 405 | Evolver结果被丢弃 | 接入Prompt/Persist | ✅ 已修复 |

---

## 版本里程碑

### 时间线总览

```
2026-04 ──────── 2026-05 ──────── 2026-06 ──────── 2026-07 ──────── 2026-10
  │                │                │                │                │
  v0.4.x           v0.5.0           v0.8.0           v0.9.0           v1.0.0
  紧急修复          Agent V2         运维工具补齐      专业深化         生产就绪
  安全加固          统一工具          Docker管理        数据库分析       覆盖率>50%
  C1/C2修复         GEP引擎          告警通知          Redis诊断       文档站
                   Skill系统
```

### 里程碑详细计划

| 版本 | 目标日期 | 核心交付物 | 验收标准 |
|------|----------|------------|----------|
| **v0.4.x** | 2026-04 | C1/C2修复, 安全加固 | CI门禁30%, 零已知漏洞 |
| **v0.5.0** | 2026-05 中 | 统一execute/transfer, GEP引擎 | Agent调用工具数减少，进化循环生效 |
| **v0.6.0** | 2026-05 底 | 种子Skill×5, Skill自动提炼 | 新任务可通过Skill加速 |
| **v0.8.0** | 2026-06 底 | Docker管理, 告警通知, 包管理器分发 | 可通过 `brew install opsxcli` 安装 |
| **v0.9.0** | 2026-07 | 原生ICMP, 数据库性能分析, Redis诊断 | 专业能力与独立工具持平 |
| **v1.0.0** | 2026-10 | 多集群K8s, 配置加密, 完整文档站 | 测试覆盖率 > 50%，可用于生产 |

### 优先级矩阵

```
短期(3-5天):     统一execute工具 + 风险审批集成
中期(1-2周):     GEP模块 + 三层记忆 + 种子Skill
长期(1-3月):     增强TUI进化展示 + LLM插件切换
```

---

## 技术债务清理计划

### 当前技术债务清单

| 类别 | 问题描述 | 优先级 | 计划清理版本 |
|------|----------|--------|-------------|
| 🔴 安全 | `plugins/nc/nc.go` 命令注入漏洞 | 🔴 紧急 | v0.4.x |
| 🧪 测试 | `plugins/` 层零测试覆盖(28个包) | 🔴 高 | v0.5.x |
| 🧪 测试 | CI 门禁仅 15%，远低于标准 | 🔴 高 | v0.4.x |
| 🧪 测试 | `internal/llm/factory_test.go` 测试失败 | 🔴 高 | v0.4.x |
| 🏗️ 架构 | Evolver 进化结果未反馈到提示词 | 🟠 中 | v0.4.x |
| 🔒 安全 | `cmd/common.go` 全局密码变量 | 🟠 中 | v0.4.x |
| 🏗️ 架构 | Builtin 命令 40% 重叠冗余 | 🟠 中 | v0.5.0 |
| 🏗️ 架构 | Agent工具拆分过细，需统一接口 | 🟠 中 | v0.5.0 |
| 📦 依赖 | 部分依赖版本过旧 | 🟡 低 | v0.9.0 |
| 📝 文档 | API 文档不完整 | 🟡 低 | v1.0.0 |

### 清理策略

```
阶段零 (v0.4.x):  修复C1/C2安全漏洞，Evolver结果反馈，CI门禁→30%
阶段一 (v0.5.x):  补齐 plugins/ 层单元测试，覆盖率 > 20%
阶段二 (v0.6.x):  internal/ 层深度测试，覆盖率 > 35%
阶段三 (v0.8.x):  cmd/集成测试，覆盖率 > 45%
阶段四 (v1.0.0):  集成测试 + 端到端测试，覆盖率 > 50%
```

---

## 附录

### 功能优先级矩阵

```
                    高价值
                      │
         P0 区域       │       P1 区域
    Shell 补全         │    Docker 管理
    安装脚本           │    包管理器分发
    命令搜索           │    官网文档站
    统一execute       │    HTTP 压测
    GEP引擎           │    告警通知
                      │
低成本 ───────────────┼─────────────── 高成本
                      │
         P2 区域       │       P3 区域
    SSL 证书检查       │    云平台封装
    原生 ICMP         │    运维剧本
    Redis 诊断        │    可视化 Dashboard
    配置加密           │    LLM插件系统
                      │
                    低价值
```

### 相关文档

- [TDD_PLAN.md](./TDD_PLAN.md) — TDD 开发计划
- [PROJECT_EVALUATION.md](./PROJECT_EVALUATION.md) — 项目评估报告
- [CLAUDE.md](./CLAUDE.md) — 开发规范
- [docs/ARCHITECTURE.md](./docs/ARCHITECTURE.md) — 架构说明
